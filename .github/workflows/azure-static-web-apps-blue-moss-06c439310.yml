name: Build and Push Docker Image to Azure Container Registry

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Login to Azure Container Registry
        uses: azure/docker-login@v1
        with:
          login-server: ${{ secrets.portfolio.azurecr.io }}
          username: ${{ secrets.portfolio }}
          password: ${{ secrets.6Ec4D8rdg1yIt1CdFdyN0pFOGnJx0AHZKcxjNdHN4f+ACRCfySkX }}

      - name: Build and Push Docker Image
        run: |
          docker build -t ${{ secrets.portfolio }}/portfolio:V1.0 .
          docker push ${{ secrets.portfolio }}/portfolio:V1.0

      - name: Logout from Azure Container Registry
        run: docker logout ${{ secrets.portfolio }}
